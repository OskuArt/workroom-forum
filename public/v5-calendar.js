async function renderCalendar(){
  const dates=(await api("/api/dates")).dates;
  const y=calendarMonth.getFullYear(),m=calendarMonth.getMonth(),first=new Date(y,m,1),start=(first.getDay()+6)%7,days=new Date(y,m+1,0).getDate(),prevDays=new Date(y,m,0).getDate();
  let cells="";for(let i=0;i<42;i++){let d,cy=y,cm=m,out=false;if(i<start){d=prevDays-start+i+1;cm=m-1;if(cm<0){cm=11;cy--}out=true}else if(i>=start+days){d=i-start-days+1;cm=m+1;if(cm>11){cm=0;cy++}out=true}else d=i-start+1;const key=`${cy}-${String(cm+1).padStart(2,"0")}-${String(d).padStart(2,"0")}`,ev=dates.filter(x=>localKey(x.when)===key);cells+=dayHtml(cy,cm,d,out,ev)}
  const next=dates.filter(d=>["pending","confirmed"].includes(d.status)&&new Date(d.when)>new Date()).sort((a,b)=>new Date(a.when)-new Date(b.when))[0];
  document.getElementById("main").innerHTML=`<div class="sectionTitle"><div><h1>Календарь встреч</h1><p>Планируй свои свидания и встречи здесь</p></div><button class="btn accent" onclick="quickDate()">+ Предложить встречу</button></div><div class="calLayout"><div class="calBox"><div class="calHead"><button class="iconBtn" onclick="shiftMonth(-1)">←</button><h2>${first.toLocaleString("ru-RU",{month:"long",year:"numeric"})}</h2><button class="iconBtn" onclick="shiftMonth(1)">→</button></div><div class="calWeek">${["Пн","Вт","Ср","Чт","Пт","Сб","Вс"].map(x=>`<div class="week">${x}</div>`).join("")}</div><div class="calGrid">${cells}</div></div><div><div class="next">${next?`<div class="kicker">Следующий (-ая) у нас...</div><div style="height:10px"></div><img src="${next.photo}" alt=""><h2>${esc(next.person)}</h2><p>${fmt(next.when)}</p><p>${esc(cap(next.place))}</p>${statusHtml(next.status)}`:`<div class="kicker">Следующий (-ая) у нас...</div><h2>Пока никого</h2><p>Предложи встречу одному из мэтчей.</p>`}</div></div></div>`
}
function dayHtml(y,m,d,out,ev){const key=`${y}-${String(m+1).padStart(2,"0")}-${String(d).padStart(2,"0")}`,today=localKey(Date.now())===key;return `<div class="day ${out?"out":""} ${today?"today":""}" onclick="openDay('${key}')"><div class="dayNum">${d}</div>${ev.length?`<div class="small" style="margin-top:6px">${ev.map(e=>`<span class="statusDot ${e.status}Dot"></span>`).join("")}${ev.length} встреч.</div><div class="avatarStack">${ev.slice(0,4).map(e=>`<img src="${e.photo}" alt="">`).join("")}</div>`:""}</div>`}
function shiftMonth(n){calendarMonth=new Date(calendarMonth.getFullYear(),calendarMonth.getMonth()+n,1);renderCalendar()}
async function openDay(key){const ev=(await api("/api/dates")).dates.filter(d=>localKey(d.when)===key),date=new Date(key+"T12:00:00");modal(`<h2>${date.toLocaleDateString("ru-RU",{day:"numeric",month:"long",year:"numeric"})}</h2>${ev.length?`<div class="meetingList">${ev.map(e=>`<div class="meetingRow"><img src="${e.photo}" alt=""><div><h4>${esc(e.person)}</h4><p>${new Date(e.when).toLocaleTimeString("ru-RU",{hour:"2-digit",minute:"2-digit"})} · ${esc(cap(e.place))}</p>${statusHtml(e.status)}</div><div class="actions">${e.direction==="incoming"&&e.status==="pending"?`<button class="btn accent" onclick="respondDate('${e.id}','confirmed')">Принять</button><button class="btn ghost" onclick="respondDate('${e.id}','declined')">Отказаться</button>`:`<button class="iconBtn" onclick="editDate('${e.id}')">✎</button>`}${e.status==="confirmed"?`<button class="btn ghost" onclick="completeDate('${e.id}')">Состоялась</button>`:""}</div></div>`).join("")}</div>`:`<p class="muted">На эту дату встреч нет.</p>`}`)}
async function quickDate(){const m=(await api(`/api/matches?mode=${mode}`)).matches;if(!m.length)return toast("Сначала нужен взаимный мэтч");if(m.length===1)return openDateModal(m[0].matchId,m[0].person.name);modal(`<h2>С кем назначить встречу?</h2><div class="meetingList">${m.map(x=>`<button class="personPick" onclick="openDateModal('${x.matchId}','${esc(x.person.name)}')"><img src="${x.person.photo}" alt=""><span>${esc(x.person.name)}</span></button>`).join("")}</div>`)}
function openDateModal(matchId,name){const d=new Date(Date.now()+86400000);d.setHours(19,0,0,0);meetingDraft={matchId,name,selected:localKey(d),y:d.getFullYear(),m:d.getMonth(),hour:19,minute:0,place:""};renderMeetingPicker()}
function renderMeetingPicker(){const hours=Array.from({length:24},(_,i)=>String(i).padStart(2,"0")),mins=Array.from({length:12},(_,i)=>String(i*5).padStart(2,"0"));modal(`<div class="picker"><h2>Встреча с ${esc(meetingDraft.name)}</h2><div class="pickerHead"><button class="iconBtn" onclick="meetingMove(-1)">←</button><b>${new Date(meetingDraft.y,meetingDraft.m,1).toLocaleString("ru-RU",{month:"long",year:"numeric"})}</b><button class="iconBtn" onclick="meetingMove(1)">→</button></div><div class="pickerWeek">${["Пн","Вт","Ср","Чт","Пт","Сб","Вс"].map(x=>`<span>${x}</span>`).join("")}</div><div class="pickerGrid">${pickerGrid(meetingDraft.y,meetingDraft.m,meetingDraft.selected,"meeting")}</div><div class="meetingTimeRow"><div class="field"><label>Время</label><div class="timeSelects"><select id="meetingHour" class="select">${hours.map(h=>`<option ${+h===meetingDraft.hour?"selected":""}>${h}</option>`).join("")}</select><span>:</span><select id="meetingMinute" class="select">${mins.map(v=>`<option ${+v===meetingDraft.minute?"selected":""}>${v}</option>`).join("")}</select></div></div><div class="field grow"><label>Место</label><input id="meetingPlace" class="input" value="${esc(meetingDraft.place||"")}" placeholder="Кофейня, парк, бар…"></div></div><div style="height:12px"></div><button class="btn accent" onclick="saveMeetingDraft()">Отправить на согласование</button></div>`)}
function meetingMove(n){meetingDraft.m+=n;if(meetingDraft.m<0){meetingDraft.m=11;meetingDraft.y--}if(meetingDraft.m>11){meetingDraft.m=0;meetingDraft.y++}renderMeetingPicker()}
function selectMeetingDay(key){meetingDraft.selected=key;renderMeetingPicker()}
async function saveMeetingDraft(){meetingDraft.hour=+meetingHour.value;meetingDraft.minute=+meetingMinute.value;meetingDraft.place=meetingPlace.value.trim();if(!meetingDraft.place)return toast("Укажи место встречи");const when=new Date(`${meetingDraft.selected}T${String(meetingDraft.hour).padStart(2,"0")}:${String(meetingDraft.minute).padStart(2,"0")}:00`);if(when<=new Date())return toast("Выбери время в будущем");try{await api("/api/dates",{method:"POST",body:JSON.stringify({matchId:meetingDraft.matchId,when:when.toISOString(),place:meetingDraft.place})});closeOverlay();toast("Встреча отправлена на согласование");go("calendar")}catch(e){toast("Не удалось отправить встречу")}}
async function respondDate(id,status){await api(`/api/dates/${id}/respond`,{method:"PATCH",body:JSON.stringify({status})});closeOverlay();toast(status==="confirmed"?"Встреча подтверждена":"Встреча отклонена");renderCalendar()}
async function editDate(id){const all=(await api("/api/dates")).dates,e=all.find(x=>x.id===id);if(!e)return;const d=new Date(e.when);meetingDraft={editId:id,name:e.person,selected:localKey(d),y:d.getFullYear(),m:d.getMonth(),hour:d.getHours(),minute:Math.round(d.getMinutes()/5)*5%60,place:e.place};renderMeetingEditPicker()}
function renderMeetingEditPicker(){const hours=Array.from({length:24},(_,i)=>String(i).padStart(2,"0")),mins=Array.from({length:12},(_,i)=>String(i*5).padStart(2,"0"));modal(`<div class="picker"><h2>Изменить встречу с ${esc(meetingDraft.name)}</h2><p class="muted">После изменения время или место снова уйдут на согласование.</p><div class="pickerHead"><button class="iconBtn" onclick="meetingEditMove(-1)">←</button><b>${new Date(meetingDraft.y,meetingDraft.m,1).toLocaleString("ru-RU",{month:"long",year:"numeric"})}</b><button class="iconBtn" onclick="meetingEditMove(1)">→</button></div><div class="pickerWeek">${["Пн","Вт","Ср","Чт","Пт","Сб","Вс"].map(x=>`<span>${x}</span>`).join("")}</div><div class="pickerGrid">${pickerGrid(meetingDraft.y,meetingDraft.m,meetingDraft.selected,"meetingEdit")}</div><div class="meetingTimeRow"><div class="field"><label>Время</label><div class="timeSelects"><select id="meetingHour" class="select">${hours.map(h=>`<option ${+h===meetingDraft.hour?"selected":""}>${h}</option>`).join("")}</select><span>:</span><select id="meetingMinute" class="select">${mins.map(v=>`<option ${+v===meetingDraft.minute?"selected":""}>${v}</option>`).join("")}</select></div></div><div class="field grow"><label>Место</label><input id="meetingPlace" class="input" value="${esc(meetingDraft.place||"")}"></div></div><div style="height:12px"></div><button class="btn accent" onclick="saveMeetingEditDraft()">Отправить изменения</button></div>`)}
function meetingEditMove(n){meetingDraft.m+=n;if(meetingDraft.m<0){meetingDraft.m=11;meetingDraft.y--}if(meetingDraft.m>11){meetingDraft.m=0;meetingDraft.y++}renderMeetingEditPicker()}
function selectMeetingEditDay(key){meetingDraft.selected=key;renderMeetingEditPicker()}
async function saveMeetingEditDraft(){const when=new Date(`${meetingDraft.selected}T${meetingHour.value}:${meetingMinute.value}:00`),place=meetingPlace.value.trim();if(when<=new Date()||!place)return toast("Проверь дату, время и место");await api(`/api/dates/${meetingDraft.editId}`,{method:"PATCH",body:JSON.stringify({when:when.toISOString(),place})});closeOverlay();toast("Изменения отправлены на согласование");renderCalendar()}
async function completeDate(id){await api(`/api/dates/${id}/complete`,{method:"PATCH",body:"{}"});closeOverlay();toast("Встреча добавлена в историю");renderCalendar()}
