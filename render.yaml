services:
  - type: web
    name: workroom-forum
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node -r ./brevo-fetch-shim.js -r ./vacancy-monitor.js -r ./job-maintenance.js -r ./social-features.js server.js
    healthCheckPath: /healthz
    autoDeployTrigger: commit
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: workroom-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: SITE_NAME
        value: WORK//ROOM
      - key: ADMIN_EMAIL
        sync: false
      - key: BREVO_API_KEY
        sync: false
      - key: BREVO_FROM_EMAIL
        sync: false

      # Legacy Jobicy/Arbeitnow importer in server.js is intentionally disabled.
      # Vacancy monitoring now runs through vacancy-monitor.js only.
      - key: ENABLE_JOB_IMPORT
        value: false
      - key: VACANCY_MONITOR_ENABLED
        value: true
      - key: VACANCY_MAX_AGE_DAYS
        value: 21

      # hh.ru: multilingual role + vacancy queries.
      - key: HH_PERIOD_DAYS
        value: 3
      - key: HH_QUERY_LIMIT
        value: 60
      - key: HH_DETAIL_LIMIT
        value: 260

      # Telegram: public partner channels monitored through their public t.me pages.
      - key: TELEGRAM_JOB_CHANNELS
        value: "mirkreatorovjob,designhunters,jun_hi_vacancies,serbia_vacancies,rabotavserbii,desivr_design"

      # Instagram: partner accounts + rotating multilingual vacancy hashtags.
      # Four new hashtags/day stays below the rolling unique-hashtag allowance.
      - key: INSTAGRAM_JOB_ACCOUNTS
        value: "vacancy_design,simple_studio,beginit.indrive.ca"
      - key: INSTAGRAM_HASHTAG_BUDGET
        value: 4
      - key: INSTAGRAM_ACCESS_TOKEN
        sync: false
      - key: INSTAGRAM_IG_USER_ID
        sync: false

databases:
  - name: workroom-db
    plan: free
    databaseName: workroom
    user: workroom
